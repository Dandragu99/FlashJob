# Antes de empezar a trabajar
git pull origin dev --rebase  # A veces no se sube y hay que usar el --reabase

# Luego después de trabajar,
git add .
git commit -m "lo que hice"

# Y finalmente para subirlo. Pero normalmente add, commit y push pero si no funciona hay que hacer lo de arriba y ya se sube.
git push origin dev